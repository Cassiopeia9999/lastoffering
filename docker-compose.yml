services:

  # ── MySQL 数据库 ─────────────────────────────────────────
  mysql:
    image: mysql:8.0
    container_name: lostfound_mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-123456}
      MYSQL_DATABASE: ${DB_NAME:-lostfound}
      # DB_USER=root 时不能设置 MYSQL_USER（root 是内置超级用户），直接用 root 连接即可
    volumes:
      - mysql_data:/var/lib/mysql
    # ports:
    #   - "3306:3306"    # 默认不暴露，避免与本地MySQL冲突；如需外部访问可取消注释
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${DB_PASSWORD:-123456}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ── FastAPI 后端 ─────────────────────────────────────────
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    container_name: lostfound_backend
    restart: unless-stopped
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      DB_HOST: mysql
      DB_PORT: ${DB_PORT:-3306}
      DB_USER: ${DB_USER:-root}
      DB_PASSWORD: ${DB_PASSWORD:-123456}
      DB_NAME: ${DB_NAME:-lostfound}
      SECRET_KEY: ${SECRET_KEY:-please-change-this-secret-key-in-production}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-60}
      UPLOAD_DIR: media/images
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost}
    volumes:
      - media_data:/app/media/images
    # GPU 支持（要求宿主机安装 nvidia-container-toolkit）
    # 若无 GPU 可删除 deploy 段
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    ports:
      - "8000:8000"      # 可按需注释掉，仅内部访问
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 60s

  # ── Nginx 前端 ──────────────────────────────────────────
  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
    container_name: lostfound_frontend
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "80:80"

volumes:
  mysql_data:
    driver: local
  media_data:
    driver: local
